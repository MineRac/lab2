generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  name      String?
  role      Role     @default(VIEWER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  autoOrders AutoOrder[]
}

model Product {
  id          String   @id @default(cuid())
  sku         String   @unique
  name        String
  category    String
  price       Float
  cost        Float?          // себестоимость
  stock       Int      @default(0)
  minStock    Int      @default(10)   // порог для авто‑заказа
  maxStock    Int      @default(100)
  location    String?           // стеллаж, ряд
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  movements   StockMovement[]
  autoRules   AutoOrder[]
  orders      Order[]
}

model StockMovement {
  id        String   @id @default(cuid())
  productId String
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  quantity  Int      // положительное = приход, отрицательное = расход
  type      MovementType
  note      String?
  createdAt DateTime @default(now())
}

model AutoOrder {
  id          String   @id @default(cuid())
  productId   String
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  triggerLevel Int
  orderQuantity Int
  isActive    Boolean  @default(true)
  lastTriggeredAt DateTime?
  createdBy   String?  // userId
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Order {
  id          String   @id @default(cuid())
  productId   String
  product     Product  @relation(fields: [productId], references: [id])
  quantity    Int
  status      OrderStatus
  totalPrice  Float?
  orderedAt   DateTime @default(now())
  expectedDelivery DateTime?
  deliveredAt DateTime?
  externalId  String?   // номер заказа у поставщика
}

model DemandPrediction {
  id         String   @id @default(cuid())
  productId  String
  product    Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  predictedDate DateTime
  predictedQuantity Float
  modelVersion String
  createdAt  DateTime @default(now())

  @@unique([productId, predictedDate])
}

enum Role {
  ADMIN
  MANAGER
  VIEWER
}

enum MovementType {
  INBOUND
  OUTBOUND
  ADJUSTMENT
}

enum OrderStatus {
  PENDING
  CONFIRMED
  SHIPPED
  DELIVERED
  CANCELLED
}